<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>Revisión profesional | Admin IRIS</title><link rel="stylesheet" href="{{ asset('css/global.css') }}"><link rel="stylesheet" href="{{ asset('css/dashboard.css') }}"></head><body class="dashboard-body"><main class="dashboard-main"><header class="dashboard-header"><div class="header-copy"><div class="welcome-text"><h1>{{ $user->nombre_completo }}</h1><p>{{ ucfirst($user->rol) }} · Estado: <strong>{{ $user->professional_status }}</strong></p></div></div><div class="header-actions"><a class="btn-outline" href="{{ url('/admin/profesionales') }}">Volver</a></div></header>@if(session('success'))<div class="dash-card" style="color:#1e5a45">{{ session('success') }}</div>@endif<section class="dash-card"><div class="card-top"><div><p class="section-label">Datos personales</p><h2>Contacto y cuenta</h2></div></div><p><strong>Email:</strong> {{ $user->email }}</p><p><strong>Teléfono:</strong> {{ $user->telefono ?? 'No capturado' }}</p><p><strong>Contacto emergencia:</strong> {{ $user->emergencyContact?->nombre ?? 'No capturado' }} · {{ $user->emergencyContact?->telefono ?? '' }}</p></section><section class="dash-card"><div class="card-top"><div><p class="section-label">Perfil profesional</p><h2>Credenciales y servicios</h2></div></div>@php($p=$user->professionalProfile)<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px"><p><strong>Título:</strong><br>{{ $p?->titulo_profesional ?? 'Pendiente' }}</p><p><strong>Especialidad:</strong><br>{{ $p?->especialidad_principal ?? 'Pendiente' }}</p><p><strong>Cédula:</strong><br>{{ $p?->cedula_profesional ?? 'Pendiente' }}</p><p><strong>Institución:</strong><br>{{ $p?->institucion ?? 'Pendiente' }}</p><p><strong>Experiencia:</strong><br>{{ $p?->experiencia_anios ?? '—' }} años</p><p><strong>Modalidad:</strong><br>{{ $p?->modalidad ?? '—' }}</p><p><strong>Tarifa:</strong><br>${{ $p?->costo_min ?? '—' }} - ${{ $p?->costo_max ?? '—' }}</p></div><p><strong>Biografía:</strong><br>{{ $p?->biografia ?? 'Pendiente' }}</p><p><strong>Presentación:</strong><br>{{ $p?->presentacion ?? 'Pendiente' }}</p></section><section class="dash-card"><div style="display:flex;gap:12px;flex-wrap:wrap"><form method="post" action="{{ url('/admin/profesionales/'.$user->id.'/aprobar') }}">@csrf<button class="btn-primary" type="submit">Autorizar perfil</button></form><form method="post" action="{{ url('/admin/profesionales/'.$user->id.'/rechazar') }}" style="display:flex;gap:8px;align-items:center;flex-wrap:wrap">@csrf<input name="reason" required placeholder="Motivo de rechazo" style="padding:12px;border:1px solid #ddd;border-radius:10px;min-width:280px"><button class="btn-outline" type="submit">Rechazar</button></form></div></section></main></body></html>
