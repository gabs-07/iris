<!DOCTYPE html>
<html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Reportes de comunidad | IRIS</title><link rel="stylesheet" href="{{ asset('css/global.css') }}"><link rel="stylesheet" href="{{ asset('css/dashboard.css') }}"></head><body class="dashboard-body"><main class="dashboard-main" style="padding:2rem;max-width:1000px;margin:auto;"><h1>Reportes de comunidad</h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@forelse($reports as $report)<article class="dashboard-card" style="padding:1rem;margin:1rem 0;"><h2>{{ $report->post?->title ?? 'Publicación eliminada' }}</h2><p><strong>Motivo:</strong> {{ $report->reason }}</p><p>{{ $report->details }}</p><p><small>Reportado por: {{ $report->user?->nombre_completo }} · Estado: {{ $report->status }}</small></p>@if($report->post)<blockquote>{{ $report->post->content }}</blockquote>@endif@if($report->status==='pending')<form method="POST" action="{{ route('admin.comunidad.reportes.resolve',$report) }}" style="display:flex;gap:.5rem;">@csrf<select name="action"><option value="keep">Mantener publicación</option><option value="hide">Ocultar publicación</option><option value="delete">Eliminar publicación</option></select><button class="btn-primary-small">Resolver</button></form>@endif</article>@empty<p>No hay reportes.</p>@endforelse{{ $reports->links() }}</main></body></html>
